{
  "name": "mothershiprpg",
  "version": "0.0.0",
  "description": "The unofficial Mothership system for Foundry VTT",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "design": "vite --config vite.design.config.ts",
    "design:build": "vite build --config vite.design.config.ts",
    "watch": "vite build --watch",
    "clean": "rm -rf dist",
    "content": "node scripts/build-content.ts",
    "setup": "node scripts/setup.ts",
    "deploy": "node scripts/deploy.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit && svelte-check --tsconfig ./tsconfig.svelte.json",
    "test:e2e": "./scripts/e2e-preflight.sh && npm run build && ./scripts/packs.sh pack && npm run setup && playwright test",
    "test:e2e:run": "playwright test",
    "test:e2e:setup": "node scripts/setup-test-env.ts",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "test:foundry": "bash scripts/start-test-env.sh",
    "check:e2e": "tsc --noEmit -p test/e2e/tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@types/node": "^22.12.0",
    "jsdom": "^29.1.1",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.5",
    "typescript": "^5.9.2",
    "vite": "^8.0.0",
    "vitest": "^4.1.9"
  }
}
